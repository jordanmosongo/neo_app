import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 50,
    height: 50,
    borderTopColor: 'transparent',
    borderWidth: 4,
    borderColor: '#271d67',
  }
})